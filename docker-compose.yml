services:
  client:
    build:
      context: ./client
    ports:
      - 4200:4200
    depends_on:
      - server
  server:
    build:
      context: ./server
    ports:
      - 8080:8080
    volumes:
      - ./server/prisma:/app/prisma

    environment:
      DATABASE_URL: ${DATABASE_URL}
